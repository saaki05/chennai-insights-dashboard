# Lean runtime dependency set for the deployed service (Render/any host).
# Excludes notebook-only packages (jupyter, matplotlib, seaborn) which are
# only needed locally for notebooks/research_analysis.ipynb — see
# requirements.txt for the full local-dev set.
fastapi==0.115.0
uvicorn[standard]==0.30.6
scikit-learn==1.5.2
numpy==2.1.1
pandas==2.2.3
pydantic==2.9.2
python-dotenv==1.0.1
aiosqlite==0.20.0
